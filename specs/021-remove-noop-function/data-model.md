# Data Model: Remove No-Op Function

## Entities

**None.** This feature involves no data entities, no new fields, no schema changes, and no state transitions.

The change is purely source code cleanup: removing a dead function definition and its call sites from `src/views/fragments.ts`.
