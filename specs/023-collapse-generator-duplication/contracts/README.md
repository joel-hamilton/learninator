# Contracts

This refactoring has no external-facing contracts. The public API of `LessonGenerator` (four public methods, `getJobStatus`, the `JobStatus` type, and the `createLessonGenerator` factory) is unchanged. All changes are internal to `src/lessons/generator.ts`.
