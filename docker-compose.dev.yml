version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./package.json:/app/package.json
    env_file:
      - .env
    command: npm run dev
