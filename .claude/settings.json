{
  "hooks": {
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "output=$(npm run test 2>&1); exit_code=$?; if [ \"$exit_code\" -ne 0 ]; then echo \"$output\"; exit 2; fi",
            "timeout": 120,
            "statusMessage": "Running tests...",
            "asyncRewake": true,
            "rewakeMessage": "Tests failed after task completion. Fix the failing tests and continue:",
            "rewakeSummary": "Test failures — fix and continue"
          }
        ]
      }
    ]
  }
}
